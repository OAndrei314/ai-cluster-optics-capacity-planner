[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-cluster-optics-capacity-planner"
version = "0.1.0"
description = "AI-cluster optical networking capacity planner"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["cluster_optics_planner*"]
